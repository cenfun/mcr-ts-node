export function sum(a: number, b: number) {
    if (a === 2 && b === 18) {
      return 20;
    }
  
    interface Something {
      name: any;
    }
  
    console.log("hello" as unknown as Something);
  
    if (a === 1 && b === 33) {
      return 34;
    }
  
    return a + b;
  }
  